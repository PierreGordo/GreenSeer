use axum::extract::State;
use axum::http::StatusCode;
use axum::{response::Html, routing::get, Router};
use minijinja::{context, Enviroment};
use std::sync::Arc;


//async
#[tokio::main]
async fn main() {
    println!("Hello, world!");
}
